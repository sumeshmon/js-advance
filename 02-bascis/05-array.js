const user = {
    name: 'Sumesh',
    age: 18,
    isLoggedIn: false
}
//const {name} = user
const {isLoggedIn} = user
const {isLoggedIn: login} = user

// console.log(isLoggedIn);
console.log(login);
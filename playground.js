const myname  = 'sumesh k sundaran'
for (const param of myname) {
    //console.log(param);
}

const myObj = {
    name : "Sumesh",
    age : 24
}

for (const key in myObj) {
    console.log(myObj[key]);
}

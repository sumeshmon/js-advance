let val = 0
while (val <= 10) {
    console.log(`value is ${val}`);
    val = val + 2
}

// array
const family = ['sumesh','sali','theertha','tejas']
let value = 0

while (value < family.length) {
    console.log(`members are ${family[value]}`);
    value = value + 1
}